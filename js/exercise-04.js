/*
    What would each of these lines of code do:

    1. true && console.log("moo");
    2. false && console.log("moo moo?")
    3. true || console.log("hello friend");
    4. false || console.log("bye friend");

    Once you see the results, write in a comment why it works that way
*/
//1 The fistrly execute the console.log with normality.
//2 The next no execute nothing beacsue false is in the line.
//3 The result is no execute becasue the left form de OR || is true and no comprove the right side.
//4 The result is execut because is false and like the left side is false comproev the other side from the OR || the right side.